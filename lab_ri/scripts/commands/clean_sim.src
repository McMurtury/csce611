#!/bin/sh

if [ $# -ge 1 ] ; then
	if [ $1 = "help" ] ; then
		echo "Clean simulation artifacts as generated by the simulate command" 1>&2
		exit 0
	fi
fi

rm -rf ./work ./transcript *.wlf
echo "cleaned simulation files" 1>&2
